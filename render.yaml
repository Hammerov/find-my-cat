services:
  - type: web
    name: find-my-cat-backend
    env: python
    plan: free
    root: backend
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    autoDeploy: true
